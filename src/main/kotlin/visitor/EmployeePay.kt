package visitor

class EmployeePay(var employeeName: String, var totalPay: Double) {

    // No es necesario definir los métodos getter y setter en Kotlin, ya que son generados automáticamente.
    // Puedes acceder directamente a las propiedades employeeName y totalPay.

    // Si deseas personalizar el comportamiento de los getters y setters, puedes utilizar las propiedades personalizadas de Kotlin.

}
